<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTube Clone</title>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <!-- Header -->
    <header class="header-container">
      <div class="header-left">
        <button onclick="window.location.href='<%= root_path %>';" class="youtube-logo">
          <i class="fab fa-youtube text-danger"></i>
          <span>YOUTUBE</span>
        </button>
      </div>
      <div class="search-container">
        <nav class="navbar">
          <div class="container-fluid">
            <%= form_with url: search_videos_path, method: :get, local: true, class: "search-form" do %>
              <%= text_field_tag :query, params[:query], placeholder: "Search..", class: "search-input" %>
              <button type="submit" class="search-button">
                <i class="fas fa-search"></i>
              </button>
            <% end %>
          </div>
        </nav>
      </div>
      <div class="user-dropdown">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="fas fa-user"></i>
        </button>
        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton1">
          <% if current_user %>
            <li><%= link_to "Edit Account", edit_user_registration_path, class: "dropdown-item" %></li>
            <li><%= button_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item", data: { turbo: "false" } %></li>
          <% else %>
            <li><%= link_to '로그인', new_user_session_path, class: 'dropdown-item' %></li>
            <li><%= link_to '회원가입', new_user_registration_path, class: 'dropdown-item' %></li>
          <% end %>
        </ul>
      </div>
    </header>

    <!-- Main Container -->
    <div class="d-flex">
      <!-- Sidebar -->
      <aside class="sidebar bg-light p-3">
        <nav class="nav flex-column">
          <%= link_to '홈', root_path, class: 'nav-link' %>
          <%= link_to 'Shorts', '/shorts', class: 'nav-link' %>
          <%= link_to 'New Main', new_main_path, class: 'nav-link' %>
        </nav>
        <%= form_with(url: categories_path, method: :post, local: true) do |f| %>
          <div class="form-group">
            <label for="category">카테고리 선택</label>
            <%= f.select :category, [["옵션 1", "option1"], ["옵션 2", "option2"], ["옵션 3", "option3"]], { prompt: "선택하세요" }, class: "form-control" %>
          </div>
        <% end %>
      </aside>

      <!-- Main Content -->
      <main class="main-content container mt-4">
        <% if @video.uploaded_video.attached? %>
          <video width="640" height="360" controls>
            <source src="<%= url_for(@video.uploaded_video) %>" type="video/mp4">
            브라우저가 비디오 태그를 지원하지 않습니다.
          </video>
        <% elsif @video.video_url.present? %>
          <iframe 
            width="640" 
            height="360" 
            src="https://www.youtube.com/embed/<%= youtube_video_id(@video.video_url) %>" 
            frameborder="0" 
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
            allowfullscreen>
          </iframe>
        <% else %>
          <p>동영상이 없습니다.</p>
        <% end %>

        <!-- 제목과 업로드 날짜는 동영상 밑에 배치 -->
        <div class="video-header">
          <h1 class="video-title"><%= @video.title %></h1>
			 <div class="video-actions">
          <p>좋아요: <span id="likes-count"><%= @video.likes %></span> | 싫어요: <span id="dislikes-count"><%= @video.dislike %></span></p>
          <%= button_to '좋아요', like_video_path(@video), method: :patch, class: "like-button" %>
          <%= button_to '싫어요', dislike_video_path(@video), method: :patch, class: "dislike-button" %>
        </div>
        </div>

        <div class="video-meta">
          <p class="video-date">업로드 날짜: <%= time_ago_in_words(@video.created_at) %> 전</p>
          <p>조회수: <%= @video.views %>회</p>
        </div>

        <!-- 좋아요/싫어요 버튼과 카운트는 동영상 밑에 위치 -->

        <hr>
        <div class="comment-section">
  <h2 class="comment-title">댓글</h2>
  <% if @comments.any? %>
    <ul class="comment-list">
      <% @comments.each do |comment| %>
        <li class="comment-item">
          <span class="comment-user"><%= comment.user.full_name %>:</span>
          <p class="comment-content"><%= comment.content %></p>
          <small class="comment-time"><%= time_ago_in_words(comment.created_at) %> 전</small>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p>댓글이 없습니다. 첫 번째 댓글을 남겨보세요!</p>
  <% end %>

  <h3 class="comment-title">댓글 작성</h3>
  <%= form_with(model: [@video, @new_comment], local: true, class: "comment-form") do |form| %>
    <div class="mb-3">
      <%= form.label :content, "댓글 내용", class: "form-label" %>
      <%= form.text_area :content, rows: 3, class: "form-control" %>
    </div>
    <div class="d-flex justify-content-end">
      <%= form.submit "댓글 남기기", class: "btn btn-primary btn-submit" %>
    </div>
  <% end %>
</div>

      </main>
    </div>
  </body>
</html>
