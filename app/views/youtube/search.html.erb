<h1>검색 결과</h1>

<% @videos.each do |video| %>
  <div class='video'>
    <h2><%= video.title %></h2>
	<p><%= video[:description] %></p>
	<img src="<%= video[:thumbnail_url] %>" alt="<%= video[:title] %> Thumbnail">
	<a href="https://www.youtube.com/watch?v=<%= video[:video_id] %>" target="_blank">비디오 시청</a>
  </div>
<% end %>